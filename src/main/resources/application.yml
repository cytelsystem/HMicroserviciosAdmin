
server:
  port: ${PORT:5000}

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/cytelsystem/BackEndII
          default-label: ServidorCentralizadoConfiguracion
          search-paths: Cajero
