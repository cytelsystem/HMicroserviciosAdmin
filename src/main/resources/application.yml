
server:
  port: ${PORT:8888}

spring:
  application:
    name: configServer
  cloud:
    config:
      server:
        git:
          uri: https://github.com/cytelsystem/ParcialBackEndII
          default-label: ServidorCentralizadoConfiguracion
          search-paths: Parcial
